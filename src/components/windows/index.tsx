import { WindowProps as WindowPropsImport, WindowComponent } from "../window";
import ImageViewer, { Props as ImageViewerProps } from "./image-viewer";

export type WindowProps = WindowPropsImport;

const windows = {
    ImageViewer,
};

export default windows;
